wikistorage value - path to directory with all notes/wikis

theme value - theme to set after running microwiki

editor - what editor will open on Alt+E in wiki - set to "Built-in" if you dont need external editor or to editor command(example: for neovim set to "nvim")
